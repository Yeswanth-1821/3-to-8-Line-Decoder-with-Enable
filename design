module decoder_3to8(
    input [2:0] in,
    input en,
    output reg [7:0] out
);
    always @(*) begin
        if (en)
            out = 1 << in;
        else
            out = 8'b0;
    end
endmodule
